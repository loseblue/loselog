# loselog 项目开发需求

## 项目背景
- 项目名称：loselog
- 基于：loselog（高性能日志查看器）
- 仓库地址：https://github.com/loseblue/loselog
- 源项目特点：稳定、成熟、经过长期验证

## 核心目标
在尽量不破坏源项目稳定性的前提下，对 loselog 进行必要的功能开发与维护。

## 强制约束

### 1. 修改量最小化
- 优先采用最小侵入式改动（Minimal Invasive Changes）
- 能通过配置、参数、小函数扩展解决的，绝不修改核心逻辑
- 能局部修改的，绝不大范围重构
- 能复用现有架构和代码风格的，绝不引入新的设计模式或框架
- 每次改动必须明确说明：
  - 为什么这个改动是必要的
  - 对现有代码的影响范围

### 2. 架构变更必须先询问
以下任何涉及架构的改动，**必须先向我确认，得到明确同意后才能进行**：
- 核心数据流 / 文件读取 / 索引机制
- 多线程 / 异步处理架构
- 搜索引擎（Hyperscan / Qt regex）相关设计
- UI 主框架或主要组件结构
- 依赖库的增减或重大版本升级
- 构建系统（CMake）的重大调整
- 任何可能影响性能或稳定性的结构性变更

### 3. 工作方式要求
- 先分析现有代码实现和设计意图，再提出方案
- 方案必须包含：改动点、影响范围、风险评估、回退方案
- 代码风格严格跟随原项目（命名、缩进、注释等）
- 优先保持与上游 loselog 的兼容性和可合并性
- 每次回答时先声明「本次改动是否涉及架构变更」，若涉及则立即停止并询问

### 4. 优先级
**最小修改 + 最高稳定性** > 功能实现速度

### 5. 回复方式
每次回答都以“HI, loseblue”开始。

---

请始终遵守以上约束进行开发与修改。
